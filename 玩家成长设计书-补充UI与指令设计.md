# 玩家成长设计书：补充 UI 设计与指令设计

## 概述

为 [玩家成长设计书](file:///workspace/设计文档/玩家成长设计书.md) 新增两章：**UI 设计**（五维雷达图）与**指令设计**（技能查看/学习，纳入全局指令层）。不参考当前代码实现的技能系统（传统 RPG 技能面板），所有内容为设计深化，不涉及 Demo 代码落地。

**已确认的决策**：
- 五维雷达图采用 **Canvas 绘制**（与战场雷达同技术路线，但为全新模块，不复用旧技能面板）。
- 技能查看/学习指令定位为 **全局指令**（任意场景可用，加入指令注册表全局层）。

## 现状分析

- [玩家成长设计书](file:///workspace/设计文档/玩家成长设计书.md) 现结构：1 概述 → 2 五维技能系统（2.2-2.6 五维技能表）→ 3 等级与经验数值模型 → 4 技能引入的机制配套 → 5 成长节奏与目标 → 6 技术实现要点 → 7 小结。**缺 UI 呈现与指令入口章节**。
- 现文档 6.1 已将数据属性改为中文段落，6.2 保持自然语言描述——新增章节延续此风格（设计层面描述，无代码块数据结构）。
- 风格参照（来自其他设计书）：
  - [战斗系统设计书 §4.1](file:///workspace/设计文档/战斗系统设计书.md#L481-L534)：Canvas 雷达 + 文字面板，文字示意用代码块。
  - [世界场景设计书 §2.4](file:///workspace/设计文档/世界场景设计书.md#L185-L228)：三级地图 UI、指令切换（`map world/region/scene`）、快捷按钮常驻、当前层高亮。
  - [世界场景设计书 §4.7](file:///workspace/设计文档/世界场景设计书.md#L548-L586)：场景指令分类两层（战斗/探索层、基地服务层），指令表格式（指令 | 服务 | 关联系统）。
  - [Demo 计划指令注册表](file:///workspace/工作计划/demo20260809/demo20260809-plan.md#L104-L108)：全局指令层 `help, save, load, status, bag, equip, unequip, look`。

## 拟改动

### 1. 在玩家成长设计书新增「7 UI 设计」章节（在现有 6 技术实现要点之后）

内容要点：

**7.1 五维雷达图（Canvas，仅总览）**
- 五边形雷达图：五个轴 = 社会/知识/导航/操作/工艺，轴端为维度名；原点向外为等级刻度 1-15。
- 当前等级以**填充多边形**表示（顶点 = 各维当前等级映射位置），外圈淡环 = 满级 15。
- **经验进度**：沿每轴外侧叠加细进度条/弧线表示当前级经验进度（对齐 3.2 经验值表）。
- **不显示技能**：雷达图仅作维度等级/经验的总览，不放技能节点，避免信息庞杂（技能列表通过指令查询呈现，见 8.2）。
- 绘制说明：Canvas 绘制，与战场雷达同技术路线但独立模块；配色沿用 UI 颜色变量（[ui-colors.css](file:///workspace/mud-game-engine/assets/ui-colors.css)），维度轴用可区分色。
- 页面构成（延续"文字 + Canvas + 结构化面板"组合）：顶部为文字版维度总览（各维等级/经验/可用技能点），中部 Canvas 雷达。

**7.2 技能列表与学习界面（并入指令查询结果）**
- 技能列表**不作为独立页面**，通过指令查询结果呈现（`skills <维度>` / `learn` 反馈均以文本形式输出到查询窗口，对齐现有右侧查询面板的分流模式）。
- 查询结果按维度分组展示：编号、名称、类型徽标（解锁/机制/数值）、效果简述、学习状态（未学/已学）、技能点余额。
- 学习入口：`learn <技能>` 指令消耗 1 技能点；成功/失败反馈（点数不足、已学、未知技能）经消息系统输出，并刷新技能点余额显示。

**7.3 与其他 UI 的集成**
- 状态面板（status）可内嵌雷达缩略图；雷达图通过全局指令打开，常驻快捷按钮一行（对齐 §2.4 三级地图的快捷按钮模式）。

### 2. 新增「8 指令设计」章节（紧随 7 UI 设计）

内容要点：

**8.1 指令定位**
- 技能查看/学习指令属于**全局指令**（任何场景可用），加入指令注册表全局层（对齐 Demo 计划指令注册表 global 层，作为后续正式版全局指令的一部分）。

**8.2 指令清单**（表格：指令 | 功能 | 参数 | 关联系统）

| 指令 | 功能 | 参数 | 关联系统 |
|------|------|------|----------|
| `skills` / `技能` | 打开五维雷达图（等级/经验总览） | 无 | 成长 UI |
| `skills <维度>` / `技能 <维度>` | 查询指定维度技能列表与状态（文本输出到查询窗口） | 维度名/别名 | 成长系统 |
| `learn <技能>` / `学习 <技能>` | 消耗 1 技能点学习指定技能 | 技能名/编号 | 成长系统 |

- 维度参数取值：社会/知识/导航/操作/工艺（支持中文别名与首字匹配，对齐现有指令模糊匹配习惯）。
- `skills` 无参数 → 雷达图总览；`skills 知识` → 文本输出该维技能列表（含编号/名称/类型/效果/学习状态/技能点余额）。
- `learn` 校验逻辑：技能存在 → 未学 → 点数 ≥1 → 学习成功；否则给出明确错误提示（未知技能 / 已学 / 技能点不足）。
- 明确**不提供遗忘/洗点指令**（与 3.6 宽松节奏一致，避免数值滥用）。

**8.3 与指令注册表的集成**
- 按世界场景 §4.7.2 统一接口，全局指令注册到全局层，注册表为后续正式版 `global-commands.js` 预留条目（设计层面说明即可，无代码）。

### 3. 同步更新文档其余部分

- **目录**：补充 `7 UI 设计（五维雷达图）`、`8 指令设计`、`9 小结`（原 7 小结顺延为 9），并给出子标题锚点。
- **7 小结 → 9 小结**：章节序号顺延；在条目中补充"UI 与指令：五维雷达图总览、技能列表经指令查询呈现、全局指令入口"一句。
- **1.1 设计目标**：如该节提及"涵盖……"，可在末尾追加一句"并定义其 UI 呈现与指令入口"（可选，保持最小改动）。
- **1.4 设计约束**：不修改（约束适用于技能内容，不涉及 UI/指令呈现）。

## 假设与决策

- **假设 1**：UI/指令章节放在 6 技术实现要点之后、小结之前（与其他设计书"UI 显示在中部"略有差异，但保证最小改动；若你更希望放 2 之后，可调整，改动量很小）。
- **决策 1**：雷达图五轴=五维，等级 1-15 刻度；不做六边形"战斗力综合"等附加轴（保持五维纯粹）。
- **决策 2**：不提供洗点/遗忘指令（防滥用，与 3.6 一致）。
- **决策 3**：指令命名沿用现有中英双语风格（`skills`/`技能`、`learn`/`学习`）。
- **决策 4**：新增章节**不出现代码数据结构代码块**（对齐 6.1/6.2 中文段落风格），Canvas 仅作绘制方式描述。
- **决策 5**：不修改 mud-game-engine 代码；本次纯设计文档改动。

## 验证步骤

1. 通读新增的 7、8 章节与目录，确认锚点（`#7-ui-设计`、`#8-指令设计`）与章节标题一致、可跳转。
2. 交叉检查表述一致性：3.6（技能点规则：1 点/技能、无顺序）、2.1（技能类型标注：解锁/机制/数值）与 7.2/8.2 的学习校验描述一致。
3. 检查 `git diff`，确认本次仅改动玩家成长设计书一个文件。
4. 按用户规则推送到 GitHub 分支 `trae-game-design`。